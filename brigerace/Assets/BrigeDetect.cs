using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BrigeDetect : MonoBehaviour
{
    // Start is called before the first frame update
    public Brige brige;
    public void Hide()
    {
        gameObject.SetActive(false);
    }
}
